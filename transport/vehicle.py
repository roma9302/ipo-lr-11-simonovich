# Добавить валидацию и методы класса
class Vehicle():
	def __init__(self,vehicle_id,capacity,current_load,clients_list):
		self.vehicle_id = vehicle_id
		self.capacity = capacity
		self.current_load = current_load
		self.clients_list = clients_list


